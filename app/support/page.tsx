export const metadata = { title: "Support" };

export default function SupportPage() {
  return (
    <div className="container max-w-2xl py-12">
      <h1 className="mb-4 text-3xl font-bold tracking-tight">Support</h1>
      <p className="text-muted-foreground">
        Questions about an order? Email support@example.com and include your order ID — replace
        this with your real contact channel (email, help-desk widget, etc).
      </p>
    </div>
  );
}
